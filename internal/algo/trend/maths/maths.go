package maths

import "github.com/go-gota/gota/dataframe"

func LogDataFrame(data any) {
	df := dataframe.DataFrame{}
	_ = df
	//val := reflect.ValueOf(data)
	//switch reflect.TypeOf(data).Kind() {
	//case reflect.Slice:
	//
	//	for r := 0; r < val.Len(); r++ {
	//		valColAny := val.Index(r)
	//		valCol := reflect.ValueOf(valColAny)
	//		switch expr {
	//
	//		}
	//
	//
	//	}
	//}
}
